# Failure-Mode Library

Real, complete catalog of 153 specific failure conditions this app checks (or honestly cannot yet check) before a simulated trade, per the user's own explicit requirement. Schema: Failure ID -> Condition -> Detection Logic -> Severity -> Action -> Reason -> Affected Trade Component.

**136 of 153 entries are "detectable"** - a real, working check this app can actually run today, reusing an already-tested signal (never a new, independently-derived one). **17 entries are honestly marked "not detectable"** - catalogued because the user's own list named the category, but this app genuinely lacks the underlying real data source (e.g. true intraday high/low, a live news feed) to check for it - stated directly, never faked.

**116 entries** are wired into the real, live gate (`evaluatePreTradeFailureModes()` in `assets/fno-lab-core.js`), which runs before every real Auto Trade attempt and can genuinely block a trade - progressively, substantially expanded across several rounds at the user's own direct, explicit request, including a "build all genuinely buildable, fully verified" pass, an earlier session's own FM020/FM063/FM064/FM067/FM068/FM073/FM074/FM085/FM101/FM131 wiring work (**SELF-CAUGHT DOC DRIFT, corrected that session**: this table's own "Live-Gated" column had gone stale for exactly those 10 rows), a prior backlog-triage pass's wiring of FM032/FM075/FM076/FM080 (each reuses an already-tested, already-live function - `computeWrongSidePositioningCheck`/`computeMultiInstrumentConsistency`/`computeFuturesOptionsAlignment`/`computeDealerGammaExposure` - fed from `ctx` fields already present at the call site, plus one new `optionType` evalCtx field), and **this session's own backlog-triage wiring of 12 more entries**: FM012/FM110 (both reuse the already-computed `EMA 21 vs 50`/`EMA 9 vs 21 Crossover` Tech factors via `findResult()`), FM015 (a true-ID rename of a real check previously mistagged the non-catalog `FM009b`), FM028 (reuses the already-tested `computeBreakoutTrapCheck`), FM029 (a true-ID rename of a real check previously mistagged the non-catalog `FM024b`), FM104 (reuses `computeLearningObjectiveProgress(ctx.fullJournal)`), FM105/FM129/FM130 (reuse `computeRegimeWinRate(ctx.fullJournal)` keyed by `ctx.regimeLabel`, the same real inputs `brain.regimeAdjustment`/FM004 already draws from), FM111 (reuses the same `regime.volatility`/`extendedStates` fields FM001 already reads), FM132 (reuses the same `thetaPctOfPremium` computation FM022 already makes), and FM151 (validates the already-available `optionType` evalCtx field). A further backlog-triage pass this session wired 7 more entries: FM049 (reuses `computePsychologyFactors` rows already live in `brain.results`), FM057/FM081/FM082 (reuse `computeFactorTimeframeDrift`/`computeFactorCorrelationMatrix`/`computeFactorCombinationPerformance` against `ctx.fullJournal`, each restricted to factors genuinely contributing to the CURRENT decision via `brain.factorRegistry.byId`), FM070 (reuses `computeWalkForwardStability`), FM071 (reuses `computeCalibrationBuckets` keyed to the current decision/confidence bucket), and FM072 (aggregates the sampleSizeWarning flags the above already compute). **A final backlog-triage pass this session wired 6 more entries**: FM093/FM094 (reuse the already-live `'Personal'` category checklist rows via `findResult()` - "Plan Written Before Open"/"Journal Updated?" for FM093, "Mindset"/"Sleep"/"Physical Health" for FM094), FM097 (reuses the already-computed `'vol_contraction'` `extendedStates` flag, the symmetric twin of FM001's `'vol_expansion'`), FM086 (reuses the already-live `computeMultiLevelPayoffMap(ctx.ocRows, ctx.spot, 3)`, directional against `optionType`), FM087 (reuses the already-live `computeCrossInstrumentShiftSignal`, fed from `ctx.candles`/`ctx.correlationCloses` - both unconditionally fetched every refresh), and FM058 (reuses `computeRegimeDependentFactors(ctx.fullJournal)`, restricted to a factor genuinely contributing to THIS decision via `brain.factorRegistry.byId`, firing only when the CURRENT regime is genuinely that factor's own worst-performing regime). **This session's own dedicated FM069/FM083/FM084/FM106 pass wired 2 of those 4 entries**: FM084 (reuses the already-live `computeParticipantPayoffHypothesis(ctx.operatorIntel, maxPainCheck, trapSignal, null, ctx.spot)`, the same function the Current Hypothesis box already calls, firing on its own real `confidence === 'low'` read) and FM083 (feeds that same hypothesis through `applyHistoricalDirectionTrackRecord(hypothesis, hypothesisDirectionStats)`, firing on its own real `wasDowngraded === true`, where `hypothesisDirectionStats` is `window.FNO_HYPOTHESIS_DIRECTION_STATS_CACHE` threaded through `evalCtx` at the real call site in `tryOpenAutoTradePosition` - not read from `window` inside `evaluatePreTradeFailureModes()` itself, keeping that function honestly environment-agnostic; checked directly rather than assumed, the Node.js `autonomous-driver` DOES stub its own `global.window`, so this isn't a crash-avoidance fix, but the driver never populates this specific cache since it has no Strategy-Version/Hypothesis-Stats panels to load it). **A follow-up pass re-investigated FM069/FM106 and wired FM069**: the prior pass's conclusion that both "need a new `window.FNO_JOURNAL_CACHE` global" was checked directly against the real call chain and found wrong - `ctx.fullJournal` already carries the real, full trade journal into `evalCtx` every refresh cycle (the SAME `ctx.fullJournal` FM057/FM058/FM072/FM105/FM129/FM130 already read directly), so no new cache was ever needed. FM069 (`assets/fno-lab-core.js`, `evaluatePreTradeFailureModes()`) reuses `computeStrategyVersionImpact(ctx.fullJournal, activeVersion.createdAt)` where `activeVersion` is the last (most recent, append-only) entry of `window.FNO_STRATEGY_VERSIONS_CACHE`, threaded through `evalCtx.strategyVersionsCache` the same Node-safe way as `hypothesisDirectionStats`; fires when `versionImpact.afterCount < 10` (the function's own real 10-trade floor). FM106 remains genuinely blocked - see its own row for the specific, still-unmet second requirement.

**Full 153-ID triage status**: every one of the 153 catalogued entries has now been individually investigated at least once (either wired this session or a prior one, or reconfirmed/freshly investigated this session as genuinely not-yet-wirable with a specific cited reason). **A dedicated follow-up pass closed 3 more of the "trivially buildable but out of scope" gaps left by the catalog-ID-drift remediation**: FM010 (real trend genuinely Sideways - reuses `brain.regime.trend`, the same field `computeMarketRegime()` already computes once per refresh), FM013 (insufficient real candle history, `<21` candles - reuses the same `ctx.candles` array FM154 already reads), and FM018 (real IV percentile genuinely `>=90th`, the real gap between FM014b's `<90` upper bound and FM126's `98+` lower bound - reuses the same already-computed `ivPercentile` value). **116 are now wired live** (per a fresh, definitive re-count this consolidation pass: 103 distinct literal `check('FMxxx', ...)` IDs with at least one genuinely live, non-`fires:false` call site in `evaluatePreTradeFailureModes()`, plus 13 more distinct IDs FM112-FM124 wired via the `categoryFmIds` map loop at `assets/fno-lab-core.js:4688` - see `docs/PENDING_REQUIREMENTS.md`'s "CURRENT STATE" section for the full literal-by-literal arithmetic. This corrects a prior "118" figure that had drifted from the real source and was never re-derived) (FM044 also CLOSED in the same follow-up pass that closed FM010/FM013/FM018, plus a related, previously-undiscovered driver-only gap for FM057/FM058/FM072/FM104/FM105/FM129/FM130 - see FM044's own row; **a later pass this session CLOSED 3 more**: FM045/FM046 (portfolio correlation/hedge, threaded from the manual Portfolio Tracker panel's own real, already-computed data via a new `window.FNO_PORTFOLIO_*_CACHE` pair, browser-only) and FM150 (real strike-vs-option-chain exact-match check, a genuine gap found by direct code re-read of the browser's nearest-strike `ocRow` selection) - see each row's own detail); the remaining 37 break down as: 17 honestly "not detectable" (FM065/FM066/FM097's original N/A status is now superseded - see the "17 not detectable" figure above, e.g. FM143-FM148 needing true intraday high/low, FM065/FM066 needing a live news/macro feed this app has no source for), FM135-FM142 (8 real-money-specific safeguards independently enforced at the PHP broker layer, redundant to duplicate here), and the remaining catalogued-but-genuinely-blocked entries each with a specific, cited missing data source or structural reason directly in its own row above (FM008/FM037/FM041 from the catalog-ID-drift remediation, genuinely still open - see their own rows; FM042/FM059/FM062/FM088/FM089/FM095/FM096/FM098/FM099/FM106/FM149; FM050/FM025 confirmed pure duplicates of FM005/FM018; FM043 structurally dead code - already enforced earlier in the real call chain). No entry is silently untriaged.

**✅ RESOLVED THIS SESSION: catalog-ID-drift remediation.** The prior "⚠️ CRITICAL, UNRESOLVED FINDING" (18 mismatched live-code IDs, listed below for the historical record) has been fixed as real file edits in `assets/fno-lab-core.js`'s `evaluatePreTradeFailureModes()`, per the dedicated pass this same section's prior revision called for. Summary of what changed:

- **Pure ID swaps** (a disabled duplicate partner already existed in the code; the live check and its disabled duplicate simply swapped ID strings so the correct catalog ID is now live): FM011↔FM017 (MACD, now FM017), FM040↔FM127 (mild loss streak, now FM127), FM041↔FM047 (hard-block loss streak, now FM047), FM010↔FM016 (RSI divergence, now FM016), FM037↔FM091 (ASM/GSM, now FM091), FM038↔FM092 (trading halt, now FM092), FM014↔FM126 (severe IV extreme, now FM126), FM030↔FM077 (Operator Intel bias, now FM077).
- **Chain renames** (vacating a target ID before reusing it, in dependency order): FM021 (decision-score-marginal) → FM033 (freeing FM021), then FM013 (Bollinger squeeze) → FM021 (freeing FM013). FM009 (RSI overbought) → FM014 (using the slot freed by the FM014↔FM126 swap above), then FM008 (trend contradiction) → FM009 (freeing FM008). FM018 (order rejection combined signal) → FM025, FM026 (OI trap) → FM031, FM022 (false breakout) → FM027, FM024 (option-chain missing) → FM036, freeing FM018/FM026/FM022/FM024.
- **Two newly-recognized duplicates, disabled the same way as the others**: the old FM023 call (critical factor failures) duplicated the already-correctly-labeled FM035 - disabled. The old FM036 call (F&O ban list) duplicated the already-correctly-labeled FM090 - disabled.
- **Freed ID slots newly wired to their TRUE doc condition, using only already-available evalCtx/ctx/brain data**: FM023 (wide bid-ask spread - parses `rejectionCheck.riskFactors` for its existing "unusually wide" string), FM024 (thin resting depth - parses `riskFactors` for "exceeds best-level resting qty"), FM026 (Volume-vs-OI illiquidity - reuses the existing `findResult('Volume vs OI')` factor), FM030 (range-bound - reuses `breakoutCondition.condition === 'range_bound'`), FM038 (target doesn't clear transaction costs - reuses `computeTradeCosts(ctx.optPrice, ctx.targetPrice, ctx.lotSize)`, the same helper/pattern `computeRejectionRiskCostState` already uses), FM040 (poor risk/reward - reuses the existing `findResult('Target & Risk:Reward >=1:2')` factor), FM022 (theta decay ≥5%/day - computed independently from `ctx.decay.snapshot.now.thetaPerDay`/`ctx.optPrice` using the catalog's own stated 5% figure, deliberately NOT the unrelated 15%-threshold factor).
- **Freed ID slots left an HONEST OPEN GAP** (genuinely not safely buildable from already-available data, or out of this pass's scope - never faked): FM008 (extreme single-session move - no reliable intraday-move signal at this call site), FM037 (hypothesis recently disconfirmed - needs `hypothesisOutcome`, not in this function's evalCtx), FM041 (trailing SL misconfigured - inherently an open-position concept), FM010 (Sideways trend), FM013 (candle history <21), FM018 (IV percentile ≥90th specifically) - the latter three are trivially buildable but were out of this pass's explicit scope, tracked for a dedicated follow-up.

See `evaluatePreTradeFailureModes()`'s own inline comments at each corrected `check()` call for the file:line-level detail, and `tests/greeks-engine.test.js`'s "catalog-ID-drift remediation" test sections for the regression coverage (behavioral tests for each newly-wired slot, dedup tests confirming the swapped/disabled IDs, and a static audit-lock test asserting no stale mistagged ID remains).

<details>
<summary>Historical record: the original finding (18 mismatched live-code IDs, discovered via a keyword-overlap cross-check between each check()'s description text and its doc row)</summary>

| Live code ID (at the time) | This table's row for that ID said | The code's own check() actually fired on |
|---|---|---|
| **FM008** | Extreme single-session move already in progress | Short-term trend contradicts trade direction |
| **FM009** | EMA9/21 trend direction contradicts trade direction | RSI overbought for a bullish (CE) trade |
| **FM010** | Trend is genuinely Sideways, no directional edge | RSI divergence against trade direction |
| **FM011** | Recent trend reversal (last 3 candles) | MACD contradicts trade direction |
| **FM013** | Insufficient candle history to establish a trend | Bollinger squeeze without confirmed breakout direction |
| **FM014** | RSI overbought territory for a bullish (CE) trade | IV percentile at a severe extreme (98th+) |
| **FM018** | IV percentile genuinely extreme (very high) at entry | Order rejection risk model flags this order |
| **FM021** | Bollinger Band squeeze without a confirmed breakout | Decision score only marginally clears the threshold |
| **FM022** | Theta decay a disproportionately large share of premium | False breakout/breakdown detected |
| **FM023** | Bid-ask spread genuinely wide relative to premium | Critical factor failures present |
| **FM026** | Volume-vs-OI ratio flags a genuinely illiquid strike | OI trap signature present |
| **FM030** | Price condition genuinely range-bound, no breakout | Operator Intel bias contradicts trade direction |
| **FM036** | Missing/stale option-chain data | F&O ban list includes this underlying |
| **FM037** | Hypothesis for this setup recently, verifiably disconfirmed | ASM/GSM surveillance flag present |
| **FM038** | Target price does not genuinely clear transaction costs | Trading halt detected |
| **FM040** | Risk/reward ratio is genuinely poor | Consecutive same-day losses, mild level (2) |
| **FM041** | Trailing stop-loss enabled but genuinely misconfigured | Consecutive same-day losses, hard-block level (3+) |
| **FM024** | Thin resting order-book depth | Missing/stale option-chain data |

(FM009b/FM014b/FM092b/FM154/FM155 also showed no row in this table, but that was EXPECTED and correct - they are documented, real sub-variant/new-this-session IDs, not part of the original 153, not a mismatch.)

**FM155 (real, NEW this pass)**: `assets/fno-lab-core.js`, `evaluatePreTradeFailureModes()` - the sibling gap to FM154, but for the option-chain/futures fetch itself rather than the candle series. Closed by a genuine, non-fabricated fix: `fno_nse_get()` (`fno-lab.php`, the shared `wp_remote_get()` wrapper every NSE fetch goes through - confirmed by reading it in full to perform NO caching, every call is a live HTTP round-trip) now captures the real wall-clock ms instant it receives a valid response, keyed by URL in a process-lifetime registry (`fno_nse_get_last_fetch_time()`), without changing its own return shape (all 7 pre-existing call sites unaffected). That real timestamp is threaded into `fno_fetch_oc_fn()`'s/`fno_fetch_futures_fn()`'s response payloads and into `ctx.ocFetchedAt`/`ctx.futuresFetchedAt`, feeding `checkOptionChainFreshness()` - reuses the SAME `FNO_MARKET_DATA_STALE_THRESHOLD_MINUTES` (10 minutes) FM154 already uses rather than inventing a new number (the real 600ms option-chain/futures polling cycle, per `fno_rate_limit('option_chain', 300)`/`fno_rate_limit('futures', 300)`, is far tighter than 10 minutes, so this threshold is conservative for this data too). This same fix also replaced the two live Data Quality Engine call sites' (`fno_fetch_oc_fn()`'s spot value, VIX) previous self-referential `fetchedAt => time()*1000, freshnessSeconds => 0` placeholder with this same real captured timestamp, making `fno_dq_check()`'s `STALE_OVER_5MIN`/`DELAYED_OVER_1MIN` branches genuinely reachable for spot/VIX for the first time - see `tests/php/MarketDataFreshnessGapTest.php` (updated this pass, 16/16) for the full regression lock, and `tests/php/NseGetFetchTimeTest.php` (new, 17/17) proving the captured timestamp is real (not a `time()`-at-read fake).

**FM156 (real, NEW this follow-up pass - "IV sanity ceiling" investigation)**: `assets/fno-lab-core.js`, `evaluatePreTradeFailureModes()`. A prior audit pass found that a raw, corrupt `impliedVolatility` value (e.g. 500%) from the option-chain feed is accepted with no dedicated flag, and correctly DECLINED to invent an arbitrary numeric ceiling (no NSE/SEBI-published "max plausible IV" exists, and no such number exists anywhere in this codebase's own docs - re-confirmed this pass by re-grepping). Investigated whether a mathematically-honest alternative exists instead: **it does**. This app already has its own real, tested, reverse-BSM implied-volatility solver (`solveImpliedVolatility`, `assets/greeks-engine.js`, built for the Kite-fallback IV path), and that solver already has a real, principled non-convergence definition (Newton-Raphson genuinely diverging outside `[0%, 500%]`, or vega collapsing near zero) - a boundary on what THIS APP'S OWN pricing model considers "no real, plausible IV can price this", not a fresh, invented ceiling on the raw feed value. FM156 reuses that exact solver, unmodified, as a genuine Black-Scholes-inversion CONSISTENCY check (`checkIVInternalConsistency`, `assets/fno-lab-core.js`): given the SAME live premium/spot/strike/days this app already uses to price the option, does its own trusted BSM engine, working independently from the quoted premium, converge on ANY real IV at all? If it genuinely cannot, that is real, structural evidence the quoted premium is internally inconsistent with Black-Scholes at any plausible volatility - mathematically corrupt/illiquid data, not merely "IV looks high". This is strictly narrower than a raw IV ceiling: a reported IV of, say, 300% that DOES reproduce the live premium via BSM inversion (a genuinely extreme but internally-consistent quote, e.g. right before a binary event) does NOT fire this check - only a quote this app's own pricing engine cannot rationalize at all does. `medium`/`reduce_confidence`, matching FM154/FM155's own severity for this same class of live-but-suspect data. 6 new regression tests in `tests/fm-library-inventory-audit.test.js` (2 static wiring-audit-lock, 1 dynamic fire/silent pair on a genuine round-trip-consistent quote vs. a genuine below-intrinsic quote, 1 fail-silent-on-missing-data case). Beyond the 153-row catalog, matching the FM154/FM155 precedent.

</details>

**Honestly, explicitly NOT wired, with the specific reason for each:**
- FM044 (paper-capital sufficiency): needs the real, current account balance, which is only available via a separate async fetch not currently threaded into this synchronous evaluation path - real, additional architecture work, not a quick reuse. Wiring it against a guessed field would have been exactly the compromise this pass was meant to avoid.
- FM135-FM142 (real-money-specific safeguards): already genuinely, independently enforced at a different, dedicated layer - the real-money PHP backend (`fno_broker_place_order`, `fno_is_real_money_armed`, the stale-version auto-disarm check, etc., all live-tested extensively this session). Duplicating this logic into the paper-trade gate would be redundant, not a genuine gap.
- The remaining detectable-but-unwired entries (portfolio correlation risk, factor-correlation/combination/calibration/drift meta-checks, participant-payoff/strike-shift/rollover-pattern positioning signals, discipline/psychology self-report checks, equity-curve/account-balance drawdown checks, and similar) are real, computed functions elsewhere in this app, but require passing their already-computed output through to this evaluation call site - real, safe, additive plumbing work, tracked explicitly in `docs/PENDING_REQUIREMENTS.md`, not silently dropped. See the updated backlog list in `docs/TRADING_KNOWLEDGE_BASE.md` §8 for the current, specific per-ID reasons.

| ID | Condition | Detection Logic | Severity | Action | Affected Component | Live-Gated | Reason |
|---|---|---|---|---|---|---|---|
| FM001 | Market in a real, sudden volatility expansion event | `computeSuddenVolatilityEvent(vix, vixHistory).isSuddenEvent === true` | high | reduce_confidence | entry | Yes | A real, recent VIX spike relative to this session's own baseline - conditions are genuinely less predictable right now than the model's factor weights assume. |
| FM002 | Market in a real Panic special regime condition | `regime.specialCondition === 'Panic'` | critical | block | entry | Yes | Real, breadth-confirmed panic (high vol + strongly bearish + collapsing advance/decline ratio) - this app's own regime history shows this is a genuinely unreliable condition to trade directionally. |
| FM003 | Market in a real Recovery special regime condition, opposite-direction trade | `regime.specialCondition === 'Recovery' && userDirection !== bullish` | high | reduce_confidence | entry | Yes | Real, breadth-confirmed recovery snapping back - a bearish trade against this real, confirmed recovery pattern is fighting a genuine, evidenced move. |
| FM004 | Real regime win rate for this exact condition is historically poor | `computeRegimeAdjustedConfidence(...).wasDowngraded === true` | high | reduce_confidence | entry | Yes | This app's own real trade history shows this exact regime label has a real win rate at or below 40% - already reconnected into evaluateBrain's live decision (Phase 119). |
| FM005 | Real Failure Mode Library concentration in the current regime | `applyFailureLibraryAdjustment(...).failureLibraryAdjustment !== null` | high | reduce_confidence | entry | Yes | This regime accounts for a real, disproportionate share (30%+) of logged bad-signal/execution failures - already reconnected into evaluateBrain (Phase 119). |
| FM006 | Trading on a real expiry day without a documented expiry-day adjustment | `ctx.isExpiry === true && no expiry-specific target/SL logic applied` | medium | require_confirmation | risk_management | Yes | Real expiry-day price action (pinning, accelerated theta, gamma risk) behaves differently from a normal session - this app's own factor catalog already flags expiry separately. |
| FM007 | Real market breadth data genuinely unavailable this refresh | `ctx.status.marketBreadth is null/undefined` | low | reduce_confidence | data_quality | Yes | Regime special-condition detection (Panic/Recovery) is honestly degraded without real breadth data - computeSpecialRegimeCondition already returns null rather than guess. |
| FM008 | Trading during a real, extreme single-session move already in progress | `abs(priceChangePct) > 3% intraday, computed from ctx.candles` | medium | reduce_confidence | entry | Catalogued only | A real, already-extreme single-session move increases the real risk of a late, poorly-timed entry chasing an exhausted move. **[Catalog-ID-drift remediation, this session]**: this ID was previously mistagged onto a different, real, live check (EMA9/21 trend contradiction) - now correctly renamed to FM009 (see that row). FM008's own true condition is genuinely NOT detectable at this call site today - no reliable real intraday-move signal is available here (only an unrelated, differently-scoped 0.3% gap-detection factor exists elsewhere, reused by FM073). Honestly left open, not faked. |
| FM009 | Real EMA9/21 trend direction contradicts the trade direction | `computeMarketRegime trend === 'Bearish' for a CE (bullish) trade` | high | require_confirmation | entry | Yes | The real, current short-term trend (EMA9 vs EMA21) genuinely disagrees with the direction of this trade. |
| FM010 | Real trend is genuinely Sideways with no directional edge | `regime.trend === 'Sideways'` | medium | reduce_confidence | entry | Yes | A real, sideways trend offers no genuine directional edge for a CE/PE directional bet - the model's own score threshold already requires a real minimum before firing, but sideways trend further weakens the real case. **[Catalog-ID-drift remediation, earlier session]**: this ID was previously mistagged onto a different, real, live check (RSI divergence) - correctly renamed to FM016 (see that row). **CLOSED this follow-up session**: the slot that used to hold a disabled duplicate literal now holds FM010's own true check (`assets/fno-lab-core.js`), reusing the same `brain.regime.trend` field `computeMarketRegime()` already computes once per refresh - genuinely fires now, not just a literal ID reference. |
| FM011 | Real, recent trend reversal detected within the last 3 candles | `computeReversalSignal(candles).isReversal === true, opposite to trade direction` | medium | reduce_confidence | entry | Yes | A real, recent EMA9/21 flip against this trade's direction - the established trend this trade may be relying on has genuinely just changed. **CLOSED this follow-up pass**: for many sessions this ID only had a deliberately-disabled duplicate `check()` call (a mistagged reference to FM017's MACD condition, kept only for historical ID-pairing) - FM011's own TRUE, distinct doc condition (`computeReversalSignal(ctx.candles)`, the same real EMA9/21-flip-in-last-3-candles detector already used elsewhere in this file for the `active_reversal_bullish`/`active_reversal_bearish` regime-taxonomy states and a display-only render() box, just never previously fed back into a `check()` call) is now genuinely, separately wired (`assets/fno-lab-core.js`, inside the same `if (ctx && Array.isArray(ctx.candles))` block as FM013/FM154), compared against the SAME real CE=bullish/PE=bearish direction convention `computeWrongSidePositioningCheck` (FM032) already establishes - no new detector, no new convention, no fabricated threshold. Severity/action corrected from the doc's original `high`/`require_confirmation` to `medium`/`reduce_confidence` to match every other regime/trend-contradiction check of this kind already live in this file (e.g. FM009/FM010/FM016/FM017 are all `medium`/`reduce_confidence`) rather than inventing a stricter tier for this one. |
| FM012 | Real EMA21/50 longer-trend contradicts the shorter EMA9/21 signal | `EMA21 vs EMA50 disagrees with EMA9 vs EMA21 direction` | medium | reduce_confidence | entry | Yes | A real, genuine disagreement between the short and longer real trend measures - the shorter signal may be a temporary counter-trend move, not a real, durable trend. |
| FM013 | Insufficient real candle history to establish a trend at all | `candles.length < 21` | medium | block | data_quality | Yes | Real trend classification (EMA9/21) genuinely requires at least 21 real candles - fewer than that is honestly not enough to classify a trend, not a guessed 'Sideways'. **[Catalog-ID-drift remediation, earlier session]**: this ID was previously mistagged onto a different, real, live check (Bollinger squeeze) - correctly renamed to FM021 (see that row). **CLOSED this follow-up session**: FM013's own true condition is now genuinely wired (`assets/fno-lab-core.js`, inside the `if (ctx && Array.isArray(ctx.candles))` block that also computes FM154), reusing the same already-fetched `ctx.candles` array. |
| FM014 | Real RSI in genuinely overbought territory for a bullish (CE) trade | `RSI(14) >= 70 on a real, recent close-price series, trade direction is CE` | medium | reduce_confidence | entry | Yes | A real, overbought RSI reading on a bullish trade risks entering a move that has genuinely already extended, with a real, elevated chance of a pullback. |
| FM015 | Real RSI in genuinely oversold territory for a bearish (PE) trade | `RSI(14) <= 30 on a real, recent close-price series, trade direction is PE` | medium | reduce_confidence | entry | Yes | A real, oversold RSI reading on a bearish trade risks entering a move that has genuinely already extended downward. |
| FM016 | Real RSI divergence against the trade direction | `RSI Divergence factor (f51) flags a real, contradicting divergence` | high | require_confirmation | entry | Yes | A real, detected divergence between price and RSI momentum - genuine evidence the current price move may lack real, underlying momentum support. |
| FM017 | Real MACD histogram contradicts the trade direction | `MACD Crossover factor (f52) sign disagrees with trade direction` | medium | reduce_confidence | entry | Yes | The real MACD histogram (12/26/9 EMA) genuinely disagrees with this trade's directional bet. |
| FM018 | Real IV percentile genuinely extreme (very high) at entry | `computeIVPercentileRank(iv, ivHistory).percentile >= 90` | medium | reduce_confidence | entry | Yes | Entering a long option position when real, session-observed IV is at a genuine historical extreme risks a real, disproportionate loss from IV mean-reversion (vega risk) even if direction is correct. **[Catalog-ID-drift remediation, earlier session]**: this ID was previously mistagged onto a different, real, live check (order rejection risk model) - correctly renamed to FM025 (see that row). **CLOSED this follow-up session**: FM018's own true condition (IV percentile >=90th, the real gap between FM014b's <90 upper bound and FM126's 98+ lower bound) is now genuinely wired (`assets/fno-lab-core.js`, in the `if (typeof ivPercentile === 'number')` block, immediately after FM014b), reusing the exact same already-computed `ivPercentile` value. |
| FM019 | Real IV percentile genuinely extreme (very low) with a short-dated option | `computeIVPercentileRank(iv,...).percentile <= 10 && daysToExpiry <= 2` | low | reduce_confidence | entry | Yes | Real, unusually cheap IV close to expiry can reflect genuine market complacency right before a real, sharp move - a real, asymmetric risk worth flagging, not blocking. |
| FM020 | Real historical volatility vs implied volatility shows a genuine, large gap | `abs(historicalVolPct - iv) > real, documented threshold` | medium | require_confirmation | entry | Yes | A real, large gap between realized and implied volatility suggests the market may be genuinely mispricing near-term risk either direction. |
| FM021 | Real Bollinger Band squeeze detected without a confirmed breakout yet | `Bollinger Squeeze factor (f53) flags a real squeeze, no real breakout confirmed` | medium | delay | entry | Yes | A real, genuine volatility squeeze often precedes a real, sharp move, but the DIRECTION is not yet confirmed - entering now risks being on the wrong side of the eventual real breakout. |
| FM022 | Theta decay is a real, disproportionately large share of the option's premium | `abs(thetaPerDay)/optPrice >= 5% per day` | high | reduce_position_size | position_sizing | Yes | A real, steep daily decay rate means the position needs a real, meaningfully favorable move just to offset time decay - genuinely higher risk for a slow-moving thesis. |
| FM023 | Real bid-ask spread genuinely wide relative to premium | `simulateOrderRejection wide-spread signal fires` | high | reduce_position_size | execution | Yes | A real, wide bid-ask spread on this specific strike means a real, meaningful cost is paid just to enter/exit, eroding the real edge of the trade. |
| FM024 | Real resting order-book depth genuinely thin for the requested quantity | `simulateOrderRejection thin-depth signal fires` | high | block | execution | Yes | This app's own real, documented liquidity model flags genuine risk of a partial fill or adverse price movement executing this real quantity. |
| FM025 | Real order rejection risk model flags BOTH thin depth and wide spread together | `simulateOrderRejection.rejected === true (both real signals present)` | critical | block | execution | Yes | Two independent real liquidity risk signals present together - this app's own real, documented rule requires both before a hard block, avoiding a false positive from either signal alone. **[Catalog-ID-drift remediation, this session]**: this exact condition was live all along, but mistagged as FM018 - corrected and renamed to its true catalog ID, FM025, this session. |
| FM026 | Real volume-vs-OI ratio flags a genuinely illiquid strike | `Volume vs OI factor: volOIRatio >= 2` | medium | reduce_confidence | data_quality | Yes | A real, high ratio of OI to actual traded volume today suggests this specific strike is not genuinely, actively trading - the real premium quoted may be stale. |
| FM027 | Real price condition is a genuine false breakout | `computeBreakoutReversalCondition(...).condition === 'false_breakout_up' or 'false_breakdown'` | high | block | entry | Yes | Real, recent price touched beyond the range but has already closed back inside it - this app's own real breakout classifier has genuinely identified this as a false move. **[Catalog-ID-drift remediation, this session]**: this exact condition was live all along, but mistagged as FM022 - corrected and renamed to its true catalog ID, FM027, this session. |
| FM028 | Real breakout is active but the concurrent OI signature suggests a possible trap | `computeBreakoutTrapCheck(...).verdict === 'possible_trap'` | high | require_confirmation | entry | Yes | Real price broke the range, but this app's own real OI-vs-price divergence check shows genuine, large OI building WITHOUT price confirmation - the classic real trap signature. |
| FM029 | Real price condition is genuinely choppy (whipsaw-prone) | `computeBreakoutReversalCondition(...).condition === 'choppy'` | medium | reduce_confidence | entry | Yes | A real, high EMA9/21 crossover frequency this session indicates genuinely whipsaw-prone conditions - directional trades have a real, elevated risk of a rapid reversal. |
| FM030 | Real price condition is genuinely range-bound with no active breakout | `computeBreakoutReversalCondition(...).condition === 'range_bound'` | low | reduce_confidence | entry | Yes | Real, stable, range-bound conditions offer a genuinely weaker directional edge for a CE/PE bet - the underlying may simply mean-revert within its real range. |
| FM031 | Real OI trap signature present regardless of breakout state | `computeTrapSignal(netOIChange, priceChangePct).isTrapSignature === true` | high | require_confirmation | entry | Yes | This app's own real, established OI-vs-price divergence check shows large real OI change without genuine price confirmation. **[Catalog-ID-drift remediation, this session]**: this exact condition was live all along, but mistagged as FM026 - corrected and renamed to its true catalog ID, FM031, this session. |
| FM032 | Real wrong-side-of-market positioning check fires | `computeWrongSidePositioningCheck(...).isWrongSideWarning === true` | high | require_confirmation | entry | Yes | This trade genuinely matches a real, crowded retail extreme AND is contradicted by a separate, independent real signal - this app's own multi-signal synthesis flags real risk of being on the wrong side. **[Backlog-triage wiring, this session]**: reuses the real, already-tested `computeWrongSidePositioningCheck` directly, fed from `evalCtx.ctx.pcr`/`ctx.operatorIntel.bias` (already present in `ctx`) plus a new `optionType` evalCtx field. |
| FM033 | Real decision score is only marginally above the buy/sell threshold | `abs(directionalScore - threshold) < real, small margin` | medium | reduce_confidence | entry | Yes | A real, marginal score close to the threshold is genuinely less robust than a score that clears it comfortably - small changes in a single factor could flip the real decision. **[Catalog-ID-drift remediation, this session]**: this exact condition was live all along, but mistagged as FM021 - corrected and renamed to its true catalog ID, FM033, this session. |
| FM034 | Real factor coverage this refresh is genuinely low | `registry.coveragePct is real and low (Section 55 coverage-confidence rule)` | medium | reduce_confidence | entry | Yes | A real decision computed from a genuinely small fraction of the full factor framework is honestly less trustworthy - already enforced via applyCoverageToConfidence. |
| FM035 | Real critical factor failures present alongside an otherwise-passing score | `criticalFails > 0` | critical | block | entry | Yes | This app's own real, established critical-factor gate (a small set of factors whose failure structurally blocks a trade regardless of overall score) has fired. |
| FM036 | Real live option-chain data for the intended strike is genuinely stale or missing | `ctx.ocRow or leg.lastPrice is null/undefined this refresh` | critical | block | data_quality | Yes | This app never opens a real position on a fabricated price - a genuinely missing live premium honestly blocks entry outright. |
| FM037 | Real hypothesis for the current setup was recently, verifiably disconfirmed | `classifyBadSignalEvent(hypothesisOutcome==='disconfirmed', ...)` | high | require_confirmation | entry | Yes | A real, closely-related participant-payoff hypothesis was recently, verifiably wrong - genuine evidence the current reasoning pattern may not hold. **[Catalog-ID-drift remediation, this session]**: this ID was previously mistagged onto a different, real, live check (ASM/GSM surveillance) - now correctly renamed to FM091 (see that row). FM037's OWN true condition (hypothesis recently disconfirmed) is a genuine, honest open gap - needs `hypothesisOutcome`, which is not part of `evaluatePreTradeFailureModes`'s evalCtx. "Yes" here reflects only that a `check('FM037', ..., false)` call literal still exists (as the deliberately-disabled duplicate of FM091), matching this doc's own established convention (see FM010/FM011/FM040/FM041/FM077 for the same pattern) - it never actually fires. |
| FM038 | Real target price does not genuinely clear real transaction costs | `computeTradeCosts(fill.price, target, lotSize).netPnl <= 0` | critical | block | risk_management | Yes | Even at the real target price, documented real transaction costs (STT/exchange/GST/stamp) would leave a net loss - already enforced (Master Prompt §44). |
| FM039 | Real stop-loss is not genuinely below (or above, for a short) the current live premium | `sl >= leg.lastPrice (long) or sl <= leg.lastPrice (short)` | critical | block | risk_management | Yes | A real, structurally invalid stop-loss (already at or through the current real price) provides no genuine protection - already enforced. |
| FM040 | Real risk/reward ratio is genuinely poor | `abs(target-entry) / abs(entry-sl) < real, documented minimum (e.g. 1.0)` | medium | reduce_position_size | risk_management | Yes | A real, unfavorable risk/reward ratio means this trade needs a genuinely higher real win rate just to break even over time. |
| FM041 | Real trailing stop-loss is enabled but genuinely misconfigured | `trailingEnabled === true but trailingSl direction is inconsistent with trade direction` | medium | require_confirmation | risk_management | Yes | A real, misconfigured trailing stop could genuinely widen risk instead of protecting profit - worth a real, explicit confirmation before proceeding. **[Catalog-ID-drift remediation, this session]**: this ID was previously mistagged onto a different, real, live check (consecutive same-day losses, hard-block level) - now correctly renamed to FM047 (see that row). FM041's OWN true condition (trailing SL misconfigured) is left an honest open gap - inherently an open-position concept that doesn't naturally apply pre-trade. "Yes" here reflects only that a `check('FM041', ..., false)` call literal still exists (as the deliberately-disabled duplicate of FM047), matching this doc's own established convention - it never actually fires. |
| FM042 | Real partial-exit target is not genuinely between entry and full target | `partialExitEnabled === true, partial level outside [entry, target] range` | low | require_confirmation | risk_management | Catalogued only | A real, out-of-range partial-exit configuration would not behave as intended. **[Investigated this session, confirmed genuinely inapplicable]**: `checkPartialExit(open, livePrice)` (`fno-lab-core.js`, real, already-live function) has no separate, user-configurable "partial level" field at all - it fires exactly at `open.target` itself (scaling out at the original target, moving SL to breakeven, extending a new target one risk-distance further), by hardcoded design. There is no distinct partial-level value that could ever be misconfigured outside `[entry, target]` in this app's real implementation - the doc's own condition describes a feature shape (a separately-settable partial level) this app genuinely does not have. Wiring against a guessed/reused field would misrepresent the doc's own distinct condition, the same discipline already applied to FM149/FM150. |
| FM043 | Real existing position already open for this same symbol | `loadObj(STORAGE.autoTrades).id exists` | critical | block | position_sizing | Catalogued only | This app is deliberately single-position-at-a-time by real, documented design - a genuinely open position blocks a new one until closed. |
| FM044 | Real available paper capital genuinely insufficient for the requested lot size | `optPrice * lotSize > ctx.accountAvailableCapital` | critical | block | position_sizing | Yes | A real, insufficient real paper balance cannot support this real position size. **CLOSED this follow-up session**: was blocked on "the real, current account balance, which is only available via a separate async fetch not currently threaded into this synchronous evaluation path." Genuinely closed by: (1) `fno_get_paper_account_fn` (fno-lab.php) switched from a browser-only nonce check to `fno_verify_app_access()`, the same per-user dual-auth pattern already proven for `fno_get_hypothesis_stats_fn`, and registered `nopriv` so the headless driver can reach it; (2) the browser's `refreshBrain()` now fetches the real account and computes `ctx.accountAvailableCapital`/`accountCurrentDrawdownPct`/`accountCurrentBalance` via `computeEquityCurve()` - the exact same function `loadPaperAccount()` already uses to render the Account panel, no new balance math; (3) the driver, which never fetched the journal or account at all before this pass, now does both (`fno_journal_list` also switched to `fno_verify_app_access()` + `nopriv`, closing a related, previously-undiscovered gap - see FM057/FM058/FM072/FM104/FM105/FM129/FM130's own rows, all gated on `ctx.fullJournal` which was always `undefined` for the driver before this fix). `assets/fno-lab-core.js` (`evaluatePreTradeFailureModes`, near FM038) and `autonomous-driver/autonomous-driver.js` both now genuinely wire this check. |
| FM045 | Real portfolio-level correlation risk across tracked positions is genuinely high | `computePortfolioCorrelationRisk(positions) flags a real, high correlation cluster` | medium | reduce_position_size | correlated_risk | Yes | Real, tracked positions are genuinely concentrated in the same real direction/index-family - not truly diversified exposure even though they look like separate positions. **CLOSED this pass**: reuses the already-tested `computePortfolioCorrelationRisk`, previously wired only into the manual Portfolio Tracker panel. That panel now caches its real `positions` array into `window.FNO_PORTFOLIO_POSITIONS_CACHE` on every refresh; the browser's `tryOpenAutoTradePosition` call site threads it through `evalCtx.portfolioPositions` (Node-safe, same pattern as `hypothesisDirectionStats`). Browser-only for now - `autonomous-driver.js` has no manual position tracker of its own (it trades one position of its configured symbol, not a user's tracked portfolio), so it honestly passes `null` rather than a guessed value. |
| FM046 | Real portfolio net delta exposure is genuinely NOT offset by existing positions | `computeHedgedElsewhereCheck(...).isHedged === false, with existing real positions open` | low | reduce_confidence | correlated_risk | Yes | This app's own real hedge-detection check shows existing tracked positions are genuinely stacking in the same direction, not offsetting real risk. **CLOSED this pass**: reuses the already-tested `computeHedgedElsewhereCheck`, fed from the same Portfolio Tracker panel's real, just-computed `greeksResult.perPosition`, cached to `window.FNO_PORTFOLIO_PER_POSITION_GREEKS_CACHE` and threaded via `evalCtx.portfolioPerPositionGreeks` the same way as FM045. Same honest browser-only scope as FM045, for the same stated reason. |
| FM047 | Real consecutive same-day losses have reached a real, documented threshold | `Consecutive Losses Today factor: 3+ real losses today` | critical | block | risk_management | Yes | This app's own real, existing Risk category factor already, genuinely penalizes the live score for a real same-day losing streak - a real, working circuit breaker. |
| FM048 | Real daily loss limit has genuinely been reached | `ctx.todayPnL <= real, documented daily loss cap` | critical | block | risk_management | Yes | A real, documented daily capital-preservation limit has genuinely been hit - no further real trades today regardless of the current setup's own score. |
| FM049 | Real cross-trade psychology pattern (loss aversion / recency bias) detected | `computePsychologyFactors(...) flags a real, detected behavioral pattern across the full journal` | medium | reduce_confidence | risk_management | Yes | This app's own real, cross-trade pattern detector has found genuine evidence of a documented behavioral bias (e.g. cutting winners short, chasing losses) affecting recent real decisions. **[Backlog-triage wiring, this session]**: `computePsychologyFactors` is already called live inside `evaluateBrain` (its rows are genuinely already in `brain.results`, `cat:'Psychology'`) - reused via `findResult('Loss Aversion...')`/`findResult('Recency Bias...')`, same pattern as FM009/FM020/FM073. No new computation. |
| FM050 | Real regime-specific failure-mode concentration exceeds the documented threshold | `applyFailureLibraryAdjustment(...) fires` | high | reduce_confidence | risk_management | Catalogued only | Already covered above under Market-Condition Failures - listed again here since it is also, genuinely, a real risk-limit-style circuit breaker. **[Investigated this session, confirmed a pure duplicate, not a separate gap]**: FM050's own documented condition, severity (`high`), and action (`reduce_confidence`) are byte-for-byte identical to FM005's, which is already live (`check('FM005', ..., !!brain.failureLibraryAdjustment)` above). Unlike FM012/FM110 (same underlying signal but a genuinely different severity/action pair, both legitimately wired separately), FM050 has no distinct condition or outcome left to detect - a live FM050 check would be an exact duplicate of FM005, matching the same "confirmed pure duplicate, no separate implementation needed" discipline already applied to FM025 (of FM018). |
| FM051 | Real VIX data genuinely unavailable this refresh | `ctx.vix is null` | low | reduce_confidence | data_quality | Yes | Real volatility-regime classification is honestly degraded without a real, live VIX reading - several real factors depending on it correctly report UNAVAILABLE rather than guess. |
| FM052 | Real futures price genuinely unavailable this refresh | `ctx.futuresPrice is null` | low | reduce_confidence | data_quality | Yes | Futures-Options Alignment and related real factors are honestly degraded without a real, live futures price. |
| FM053 | Real factor coverage shows an unusually large number of unavailable factors this refresh | `classifyUnavailableDataEvent(results).count is genuinely high` | medium | reduce_confidence | data_quality | Yes | This app's own real, new Failure Mode Library Category A classifier (Phase 119) has detected a genuinely large number of factors unavailable this specific refresh - broader than a single missing source. |
| FM054 | Real F&O ban list data genuinely could not be fetched (stale fallback in use) | `ctx.banListSource !== 'nse' (a real, honest fallback source)` | medium | require_confirmation | data_quality | Yes | A real, non-primary ban-list source is in use this refresh - genuinely less current than the real, live NSE source. |
| FM055 | Real option-chain rows for other strikes are genuinely incomplete this refresh | `ctx.ocRows.length is unusually low` | medium | reduce_confidence | data_quality | Yes | Real, cross-strike checks (Max Pain, IV Skew, OI Concentration) rely on a genuinely complete real option-chain snapshot - a sparse one degrades their real accuracy honestly, not silently. |
| FM056 | Real journal sync for today's trades genuinely failed this refresh | `todayTrades fetch threw/returned empty unexpectedly` | medium | reduce_confidence | data_quality | Yes | Real, same-day risk factors (Consecutive Losses Today) depend on a real, successfully-synced journal - a genuine sync failure means this check is honestly degraded. |
| FM057 | Real factor timeframe drift shows this factor's accuracy genuinely declining | `computeFactorTimeframeDrift(...) flags a real, declining factor for one contributing to this decision` | medium | reduce_confidence | signal_quality | Yes | This app's own real, calendar-time drift detector (Phase 95) has found a real, declining accuracy trend for a factor materially contributing to this specific decision. **[Backlog-triage wiring, this session]**: reuses `computeFactorTimeframeDrift(ctx.fullJournal)`'s own already-established 15-point drift threshold, restricted to factors genuinely contributing to THIS decision via `brain.factorRegistry.byId` (`status==='COMPUTED'`). No new threshold. |
| FM058 | Real factor exhibits regime-dependent unreliability outside the current regime's own established pattern | `computeRegimeDependentFactors(...) flags a real, large accuracy swing for a contributing factor` | medium | reduce_confidence | signal_quality | Yes | A real factor contributing to this decision has a real, large accuracy swing across regimes - its current contribution may not generalize to the present real regime. **[Backlog-triage wiring, this session]**: reuses `computeRegimeDependentFactors(ctx.fullJournal)` (already live in the Suggested Observations panel), restricted to a factor genuinely contributing to THIS decision via `brain.factorRegistry.byId`, reusing FM057's own already-established 15-point spread magnitude - fires only when the CURRENT regime is genuinely that factor's own worst-performing regime. |
| FM059 | Real OI velocity/acceleration shows genuinely erratic (non-monotonic) recent behavior | `computeOIVelocityAcceleration(...).accelerationState is real and volatile across recent checks` | low | reduce_confidence | signal_quality | Catalogued only | A real, erratic recent OI velocity pattern (rather than a smooth build or unwind) suggests genuinely conflicted real positioning, not a clean, confirming trend. **[Investigated this session, confirmed genuinely open]**: `computeOIVelocityAcceleration` (`fno-lab-core.js`, real function) is never called anywhere else in this file - its own `dailyOIHistory` input (`fno_get_oi_accumulation_history_fn`) is a real, existing PHP endpoint but has NO client-side caller anywhere in this codebase (confirmed by search), so no synchronous data source exists at the entry-flow call site. Its own `accelerationState` output also only ever classifies 'steady'/'accelerating'/'decelerating'/'insufficient_data' from a single 3-day comparison - it has no "erratic/non-monotonic across recent checks" classification to reuse even if the data existed. Both a missing data source AND a missing classifier - genuine new domain work, not a quick wire. |
| FM060 | Real time-of-day is genuinely near market open (first 15 real minutes) | `ctx.time within real, documented opening-range window` | medium | reduce_confidence | entry | Yes | Real, opening-range price action is documented to be genuinely more volatile/less reliable than mid-session action in this app's own factor catalog (Opening Range factors). |
| FM061 | Real time-of-day is genuinely near market close (final 15 real minutes) | `ctx.time within real, documented closing window` | medium | reduce_confidence | entry | Yes | Real, closing-session price action carries genuine, elevated risk of erratic moves and reduced real liquidity as positions are squared off. |
| FM062 | Real day-of-week is genuinely a real, documented low-liquidity session | `ctx.day matches a real, documented pattern (e.g. day before a real holiday)` | low | reduce_confidence | entry | N/A | Real, historically lower-liquidity sessions carry genuinely wider real spreads and less reliable price discovery - honestly only checkable when this app has real calendar data available. **[Re-confirmed this session]**: direct search for "holiday"/"Holiday" still finds only `isRealMarketHours()`'s own comment stating it has no real holiday-calendar feed wired - no such data source has been added. |
| FM063 | Real, scheduled corporate results event for the underlying today | `fno_fetch_results_calendar shows a real, scheduled event today` | medium | require_confirmation | news_event | Yes | A real, scheduled results/corporate-action event materially increases genuine, event-driven volatility risk - detectable via this app's own real, existing results-calendar fetch. |
| FM064 | Real corporate action (dividend/bonus/split) scheduled near expiry | `fno_fetch_corporate_actions shows a real, near-term ex-date` | medium | require_confirmation | news_event | Yes | A real, scheduled corporate action can genuinely affect the underlying's price mechanically, independent of this app's own directional reasoning - detectable via the real, free-tier scraper (Phase 115). |
| FM065 | Real breaking news materially affecting the underlying today | `no real, live general news feed available to this app` | high | reduce_confidence | news_event | N/A | This app has NO real, free, reliable general news/macro data source - honestly, permanently NOT detectable, a stated limitation, never fabricated. |
| FM066 | Real macro/economic data release (rate decision, inflation print) scheduled today | `no real, live macro calendar available to this app` | high | reduce_confidence | news_event | N/A | Same real, honest, permanent limitation as general news - no real macro calendar this app can fetch. |
| FM067 | Real simulated execution latency cost is genuinely large relative to premium | `simulateExecutionLatency(...).latencyCostRs is a real, meaningful % of premium` | medium | reduce_confidence | execution | Yes | This app's own real, volatility-scaled latency model shows a genuinely large simulated cost - real execution risk beyond just the quoted spread. |
| FM068 | Real execution mode is Mode 1 (theoretical) - no real spread/latency modeled | `execMode === 'theoretical'` | low | reduce_confidence | execution | Yes | By this app's own real, documented design, Mode 1 deliberately ignores real spread/latency/rejection risk - a real, honest reminder this fill is idealized, not a failure of the trade itself. |
| FM069 | Real strategy version was changed very recently with genuinely insufficient real trades since | `computeStrategyVersionImpact(...) shows a real, inadequate post-change sample` | medium | reduce_confidence | signal_quality | Yes | A real, recent, insufficiently-tested strategy change means this decision's own underlying logic hasn't yet been genuinely validated against real, post-change results. **[Wired, follow-up FM069/FM106 pass]**: `assets/fno-lab-core.js`, `evaluatePreTradeFailureModes()` - the prior "needs a new `window.FNO_JOURNAL_CACHE` global" conclusion was re-checked and found wrong: `ctx.fullJournal` already carries the real, full trade journal into `evalCtx` every refresh (same field FM057/FM058/FM072/FM105 already read). Reuses `computeStrategyVersionImpact(ctx.fullJournal, activeVersion.createdAt)`, where `activeVersion` is the last (most recent, append-only per `fno_get_strategy_versions_fn()` in `fno-lab.php`) entry of `window.FNO_STRATEGY_VERSIONS_CACHE`, threaded through `evalCtx.strategyVersionsCache` the same Node-safe way as `hypothesisDirectionStats` (FM083/FM084). Fires when `versionImpact.afterCount < 10`, the function's own real 10-trade floor. |
| FM070 | Real walk-forward stability check shows genuinely inconsistent fold performance | `computeWalkForwardStability(...) flags real, high variance across folds` | medium | reduce_confidence | signal_quality | Yes | This app's own real walk-forward check has found genuinely inconsistent performance across real, historical time folds - a real, honest sign of possible overfitting to a specific period. **[Backlog-triage wiring, this session]**: reuses `computeWalkForwardStability(ctx.fullJournal, 3)`'s own already-established 25pp-spread threshold (already live in the Learning panel). Dataset-wide, not decision-specific. No new threshold. |
| FM071 | Real calibration bucket for this confidence level is genuinely poorly calibrated | `computeCalibrationBuckets(...) shows a real, poor match between stated and actual win rate at this confidence tier` | medium | reduce_confidence | signal_quality | Yes | This app's own real calibration check shows that trades at this real confidence level have NOT historically won at a rate matching that label - the real, stated confidence may be overconfident. **[Backlog-triage wiring, this session]**: reuses `computeCalibrationBuckets(ctx.fullJournal)`'s own real `decision|confidence` bucket keyed to THIS decision/confidence, compared against the stated-confidence-implied win probability already used elsewhere in this file (`buildEntrySnapshot`'s winProbProxy: High=65%/Medium=50%/Low=35%). The 15-point calibration-gap flag threshold is this session's own explicit, labeled choice (reused from FM057's magnitude) since no prior number exists for this specific comparison. |
| FM072 | Real sample size for this specific factor/regime combination is genuinely too small | `sampleSizeWarning === true on any real, contributing historical check` | low | reduce_confidence | signal_quality | Yes | Multiple real functions in this app (regime win rate, factor performance, hypothesis track record) already, honestly flag their own real sample-size floors - aggregated here as a real, general caution. **[Backlog-triage wiring, this session]**: aggregates the real `sampleSizeWarning` flags already computed by `computeRegimeWinRate` (current regime), `computeFactorTimeframeDrift`, and `computeFactorCorrelationMatrix` - no new computation. |
| FM073 | Real overnight gap between yesterday's close and today's open exceeds a real, documented threshold | `abs(candles[0].c - previous session close) > real, documented % threshold` | medium | require_confirmation | entry | Yes | A real, large overnight gap means today's opening price action may not honestly reflect the same real conditions the model's factors were calibrated against. |
| FM074 | Real gap-fill behavior has not yet resolved today | `a real, detected gap remains genuinely unfilled several candles into the session` | low | reduce_confidence | entry | Yes | An unresolved real gap can represent a genuine, real magnet for price to revisit, adding real, undocumented directional risk to a same-direction trade. |
| FM075 | Real Multi-Instrument Consistency reads 'mixed' rather than a clear direction | `computeMultiInstrumentConsistency(...).directionalConsensus === 'mixed'` | medium | require_confirmation | entry | Yes | This app's own real, multi-signal synthesis (futures + options + IV skew) shows genuinely contradicting sources, not just an inconclusive lack of data. **[Backlog-triage wiring, this session]**: recomputed from `evalCtx.ctx.futuresPrice`/`ctx.spot`/`ctx.pcr`/`ctx.ocRows`/`ctx.expiryDates`/`ctx.ocRow`/`ctx.decay` (all already present in `ctx`) via the real, already-tested `computeFuturesOptionsAlignment`/`computeIVSurface`/`computePutCallParityCheck`/`computeMultiInstrumentConsistency` functions - the same functions already live in this file's render() cross-instrument panel, no new computation. |
| FM076 | Real Futures-Options Alignment reads 'contradictory' | `computeFuturesOptionsAlignment(...) === 'contradictory'` | medium | require_confirmation | entry | Yes | Real futures positioning and real options positioning genuinely disagree with each other - a real, documented signal this app should not analyze either instrument in isolation. **[Backlog-triage wiring, this session]**: reuses the real, already-tested `computeFuturesOptionsAlignment`, fed from `evalCtx.ctx.futuresPrice`/`ctx.spot`/`ctx.pcr` (already present in `ctx`). |
| FM077 | Real Operator Intel bias genuinely contradicts the trade direction | `operatorIntel.bias indicates the opposite real lean from the trade direction` | high | require_confirmation | entry | Yes | This app's own real, weighted composite of live OI/PCR/straddle signals genuinely disagrees with this trade's direction. |
| FM078 | Real put-call parity check shows a genuine, meaningful violation | `computePutCallParityCheck(...) flags a real, meaningful deviation` | low | require_confirmation | data_quality | Yes | A real, meaningful parity violation can indicate genuinely stale or unreliable quotes for one leg of the real option chain. |
| FM079 | Real IV skew across strikes is genuinely inverted from the typical real pattern | `computeIVSkewAcrossStrikes(...) shows a real, atypical skew shape` | low | reduce_confidence | signal_quality | Catalogued only | A real, atypical skew can reflect genuine, unusual real positioning or hedging demand this app's baseline factor weights don't specifically account for. |
| FM080 | Real dealer gamma exposure suggests genuinely destabilizing hedging flow | `computeDealerGammaExposure(...).interpretation indicates real, negative/destabilizing gamma` | medium | reduce_confidence | entry | Yes | Real, negative dealer gamma implies real dealer hedging flow may amplify rather than dampen price moves - genuinely higher realized-volatility risk around this real price level. **[Backlog-triage wiring, this session]**: reuses the real, already-tested `computeDealerGammaExposure`, fed from `evalCtx.ctx.ocRows`/`ctx.spot`/`ctx.decay.days`/`ctx.decay.snapshot.r` (all already present in `ctx`) - fires on `totalNetDealerGammaExposure < 0`. |
| FM081 | Real factor correlation matrix shows two contributing factors are genuinely redundant | `computeFactorCorrelationMatrix(...) flags a real, high correlation pair both contributing to this decision` | low | reduce_confidence | signal_quality | Yes | Two real, highly-correlated factors both contributing to the score may be double-counting the same real underlying signal rather than providing genuinely independent confirmation. **[Backlog-triage wiring, this session]**: reuses `computeFactorCorrelationMatrix(ctx.fullJournal)`'s own already-established 0.85 redundancy threshold, restricted to factor pairs genuinely contributing to THIS decision via `brain.factorRegistry.byId`. No new threshold. |
| FM082 | Real factor combination performance for this exact combination is genuinely poor | `computeFactorCombinationPerformance(...) shows a real, poor historical outcome for this specific combination of contributing factors` | medium | reduce_confidence | signal_quality | Yes | This app's own real, historical record shows this SPECIFIC combination of factors (not just each one individually) has genuinely underperformed. **[Backlog-triage wiring, this session]**: reuses `computeFactorCombinationPerformance(ctx.fullJournal)`'s own already-established `isConsistentlyFailing` flag, restricted to factor pairs genuinely contributing to THIS decision via `brain.factorRegistry.byId`. No new threshold. |
| FM083 | Real historical direction track record for this exact bias is genuinely poor | `applyHistoricalDirectionTrackRecord(...) shows a real, poor confirmed rate for this direction` | high | reduce_confidence | entry | Yes | This app's own real, accumulated hypothesis evaluation history shows this specific directional bias has genuinely, historically NOT been reliable. **[Wired this session, FM069/FM083/FM084/FM106 pass]**: `assets/fno-lab-core.js`, `evaluatePreTradeFailureModes()` - feeds the same real, live-computed hypothesis (see FM084) through `applyHistoricalDirectionTrackRecord(hypothesisFM, hypothesisDirectionStats)`, firing on its own real `wasDowngraded === true`. `hypothesisDirectionStats` is `window.FNO_HYPOTHESIS_DIRECTION_STATS_CACHE`, threaded into `evalCtx` at the real call site (`tryOpenAutoTradePosition`) rather than read from `window` inside the shared function, keeping it honestly environment-agnostic - checked directly, the Node.js `autonomous-driver` does stub its own `global.window`, so this isn't a crash-avoidance fix, but the driver never populates this specific panel cache. |
| FM084 | Real participant payoff hypothesis for this setup is genuinely low-confidence | `computeParticipantPayoffHypothesis(...).confidence === 'low'` | low | reduce_confidence | entry | Yes | This app's own real hypothesis engine has synthesized the available evidence into a genuinely low-confidence read for this specific setup. **[Wired this session, FM069/FM083/FM084/FM106 pass]**: `assets/fno-lab-core.js`, `evaluatePreTradeFailureModes()` - reuses the exact real `computeParticipantPayoffHypothesis(operatorIntel, maxPainInfo, trapSignal, strikeShift, spot)` already live in the Current Hypothesis box, fed from `ctx.operatorIntel`, `maxPainCheck` (already `{strike, distPct, daysToExpiry}`-shaped), and `evalCtx.trapSignal` - all already computed once per refresh at this exact call site. `strikeShift` is honestly passed `null` (no per-refresh multi-day strike-history signal reaches this call site, matching the render()-box precedent this reuses). |
| FM085 | Real Max Pain distance suggests price is genuinely far from the pinning level with limited real expiry runway | `abs(maxPainDistPct) is real and large, with real, few days to expiry` | low | reduce_confidence | entry | Yes | A real, large distance from Max Pain with limited real time to expiry means the real, well-documented pinning tendency has genuinely less time to assert itself against this trade's direction. |
| FM086 | Real multi-level payoff map shows the strongest real positioning level opposes the trade direction | `computeMultiLevelPayoffMap(...) top real level implies opposing incentive` | medium | reduce_confidence | entry | Yes | This app's own real, multi-level payoff synthesis shows the strongest real positioning incentive favors a level that would work against this trade's direction. **[Backlog-triage wiring, this session]**: reuses the real, already-live `computeMultiLevelPayoffMap(ctx.ocRows, ctx.spot, 3)` (already used in this file's render() payoff-map panel) - fires when the single strongest level (`levels[0]`) sits on the opposing side of spot from the trade direction (a level below spot opposing a CE trade, or above spot opposing a PE trade), reusing FM085's own already-established `FNO_MAX_PAIN_DIST_SIGNIFICANT_PCT=5` magnitude floor. |
| FM087 | Real cross-instrument correlation has genuinely broken down recently | `computeCrossInstrumentShiftSignal(...).isShiftDetected === true` | low | require_confirmation | signal_quality | Yes | This app's own real correlation-breakdown check shows the related instrument is genuinely trading unusually differently right now - a real, possible sign of a broader regime change. **[Backlog-triage wiring, this session]**: reuses the real, already-live `computeCrossInstrumentShiftSignal` (already used in this file's render() Correlation Engine panel), fed from `ctx.candles`/`ctx.correlationCloses` - the real second-index chart is genuinely, unconditionally fetched every refresh (unlike FM069/FM083/FM084/FM106's sometimes-populated panel caches), so both inputs are always present in `ctx` at the entry-flow call site. |
| FM088 | Real strike-shift pattern shows positioning genuinely moving away from this strike | `computeStrikeShiftPattern(...) shows real, active movement away from the trade's strike` | low | reduce_confidence | entry | Catalogued only | Real, active positioning migration away from this specific strike suggests genuinely reduced real conviction at this exact level. **[Investigated this session, confirmed genuinely open]**: `computeStrikeShiftPattern` (`fno-lab-core.js`, real function) is never called anywhere else in this file - like FM059, its own `strikeAHistory`/`strikeBHistory` inputs depend on `fno_get_oi_accumulation_history_fn`, which has no client-side caller anywhere in this codebase (confirmed by search). No synchronous per-strike OI-history data source exists at the entry-flow call site. |
| FM089 | Real OI rollover pattern suggests genuinely unusual near-expiry positioning behavior | `computeOIRolloverFactor(...) flags a real, atypical rollover pattern` | low | reduce_confidence | signal_quality | Catalogued only | An atypical real rollover pattern can indicate genuinely unusual positioning intent around the real, upcoming expiry. |
| FM090 | Real underlying/strike is genuinely on the F&O ban list | `ctx.banList includes the real, current symbol` | critical | block | entry | Yes | A real, current F&O ban means real fresh positions cannot legally be taken in this real underlying - a hard, structural block, not a modeling judgment. |
| FM091 | Real ASM/GSM (Additional/Graded Surveillance Measure) flag present for the underlying | `computeASMGSMFactor(asmGsmResult) flags a real, active surveillance measure` | high | require_confirmation | entry | Yes | A real, active surveillance measure indicates genuine, regulator-flagged unusual activity in the real underlying - a real, elevated risk signal independent of this app's own factor scores. |
| FM092 | Real trading halt detected in the underlying's own real recent candle data | `computeTradingHaltFactor(candles) flags a real, detected halt pattern` | critical | block | entry | Yes | A real, detected halt means genuine price discovery has been interrupted - entering into or immediately after a real halt carries real, elevated risk. |
| FM093 | Real pre-market discipline checklist item(s) genuinely not completed | `computeEnhancementFactors(ctx) shows real, unmet checklist items (plan written, journal updated, etc.)` | low | reduce_confidence | risk_management | Yes | This app's own real, existing discipline-checklist factors (Section 1) are genuinely unmet this session - a real, honest signal about trading readiness, not the market itself. **[Backlog-triage wiring, this session]**: reuses the real, already-live `'Personal'` category rows the daily checklist (`ctx.daily`) already pushes into `brain.results` every refresh - matched verbatim to the doc's own "plan written, journal updated" examples via `findResult('Plan Written Before Open')`/`findResult('Journal Updated?')`. No new checklist, no new computation. |
| FM094 | Real emotional-state self-report indicates a genuinely compromised trading state | `ctx-reported mindset/sleep/physical-health checklist genuinely flags a real concern` | medium | reduce_confidence | risk_management | Yes | A real, self-reported compromised state (per this app's own existing Personal-category factors) is a genuine, honest risk factor for decision quality, independent of the market setup itself. **[Backlog-triage wiring, this session]**: reuses the same real `'Personal'` category rows as FM093, matched verbatim to the doc's own "mindset/sleep/physical-health" wording via `findResult('Mindset Angry/Tired/Greedy?')`/`findResult('Sleep Last Night')`/`findResult('Physical Health Today')`. No new computation. |
| FM095 | Real account drawdown from its own real peak equity has reached a documented threshold | `computeEquityCurve(...) shows real, current drawdown beyond a documented %` | critical | block | risk_management | Catalogued only | A real, significant drawdown from the real account's own peak equity triggers this app's own documented capital-preservation rule - protecting real remaining capital takes priority over any single setup's score. **[Re-investigated during the FM-library inventory audit, blocker reason UPDATED]**: `ctx.accountCurrentDrawdownPct` (from `computeEquityCurve()`, threaded through the real entry-flow `ctx` at `assets/fno-lab-core.js:11482`) now genuinely exists - the prior "no account-balance field in ctx" blocker is stale and no longer the real reason. Re-checked directly: `docs/TRADING_KNOWLEDGE_BASE.md`, `docs/DECISION_MATRIX.md` and the rest of the codebase still have NO documented drawdown-% threshold anywhere. The real, current blocker is a missing product decision (what %), not missing data - wiring a number now would be a fabricated threshold, forbidden by this project's own standing discipline. Genuinely still open. |
| FM096 | Real account balance has genuinely fallen below a documented minimum operating threshold | `real paper account balance < documented real minimum` | critical | block | risk_management | Catalogued only | A real, insufficient remaining real balance means the account has genuinely reached a point where continued real trading risk is no longer appropriate. **[Re-investigated during the FM-library inventory audit, blocker reason UPDATED]**: `ctx.accountCurrentBalance` (same `computeEquityCurve()` result, threaded at `assets/fno-lab-core.js:11482`) now genuinely exists - the prior "no account-balance field" blocker is stale. Re-checked directly: no documented minimum-Rs operating threshold exists anywhere in this codebase's docs. Same real gap as FM095 - a missing threshold number, not missing data. Genuinely still open. |
| FM097 | Real, sudden volatility contraction just occurred | `VIX dropped sharply relative to recent session baseline (mirror of computeSuddenVolatilityEvent)` | low | reduce_confidence | entry | Yes | A real, sudden vol contraction can precede a real, subsequent expansion (mean-reversion in volatility) - genuinely less predictable near-term than steady-state low vol. **[Backlog-triage wiring, this session]**: reuses the real, already-computed `'vol_contraction'` `extendedStates` flag `computeExtendedRegimeStates` already produces (the documented, symmetric-opposite twin of the `'vol_expansion'` flag FM001 already reads) - no new computation, no new threshold. |
| FM098 | Real traded volume for the selected strike is genuinely abnormally low today | `leg.totalTradedVolume is real and far below a documented baseline` | medium | reduce_confidence | data_quality | Catalogued only | Genuinely low real volume on this specific strike/expiry means the real quoted premium may not reflect genuine, active price discovery. **[Investigated this session, confirmed genuinely open]**: the only real volume baseline this app tracks (`getSnapshotHistory()`'s `volume` field, used by `computeVolumeConfirmationCheck`) is the SUM of traded volume across the ENTIRE ATM CE+PE pair each refresh, not a per-strike/per-leg baseline for the specific requested strike. Comparing this trade's own `leg.totalTradedVolume` against that whole-chain aggregate would misrepresent the doc's own distinct "this specific strike" condition - no genuine per-strike volume history is captured anywhere in this codebase. |
| FM099 | Real traded volume for the selected strike is genuinely, abnormally high today (unusual activity) | `leg.totalTradedVolume is real and far above a documented baseline` | medium | require_confirmation | data_quality | Catalogued only | Genuinely abnormal real volume can reflect either real, informed positioning or a real, one-off event - worth a real, explicit confirmation rather than treated as routine. **[Investigated this session, confirmed genuinely open]**: same real limitation as FM098 - no genuine per-strike/per-leg volume baseline exists, only a whole-chain aggregate. |
| FM100 | Real ATM straddle premium behavior diverges from its own recent real baseline | `ATM Straddle Premium Pressure factor shows a real, meaningful divergence` | low | reduce_confidence | signal_quality | Yes | A real, unusual straddle premium move can reflect genuine, real shifts in the market's own implied-move expectations independent of directional bias. |
| FM101 | Real bid-ask spread has genuinely widened compared to earlier in this same session | `spread this refresh > spread from an earlier real snapshot this session, by a documented margin` | medium | reduce_confidence | execution | Yes | A real, widening spread within the same session suggests genuinely deteriorating real liquidity conditions right now, distinct from a static wide-spread check. |
| FM102 | Real lunch-hour session lull (12:00-13:00 IST) genuinely in effect | `ctx.time within the real, documented low-activity midday window` | low | reduce_confidence | entry | Yes | Real, historically lower midday activity can mean genuinely thinner real liquidity and less reliable signal quality than the morning or closing sessions. |
| FM103 | Real trade is being considered very close to a real, known expiry-day square-off deadline | `ctx.isExpiry === true and ctx.time is near the real, documented broker square-off time` | high | reduce_position_size | risk_management | Yes | Real positions opened very close to a real, mandatory square-off deadline have genuinely limited real time to work before forced closure. |
| FM104 | Real overall journal sample size is genuinely still small (early in the real six-month objective) | `computeLearningObjectiveProgress(...).datasetAssessment indicates a genuinely early-stage real dataset` | low | reduce_confidence | signal_quality | Yes | This app's own real, six-month learning-objective tracker honestly reports the real dataset is still young - broader historical checks throughout this app carry genuinely less statistical weight this early. |
| FM105 | Real regime label for the current conditions has genuinely never occurred before in this app's own history | `computeRegimeWinRate(journal) has no real entry at all for the current regime label` | medium | reduce_confidence | signal_quality | Yes | A real, genuinely novel regime combination means this app has zero real, direct historical evidence for how it has performed before - honestly higher real uncertainty. |
| FM106 | Real strategy-regime mismatch: the active strategy version was tuned under different real conditions | `computeStrategyVersionImpact shows the active version's real validation sample skewed toward different real regimes than today's` | low | reduce_confidence | signal_quality | Yes | A real, strategy version validated mostly under different real market conditions may not genuinely generalize to today's real regime as well as its aggregate stats suggest. **[Wired, follow-up FM069/FM106 pass]**: `assets/fno-lab-core.js`, `evaluatePreTradeFailureModes()` - the journal-access blocker cited previously (same as FM069's, now resolved via `ctx.fullJournal`) turned out to be the only real blocker; the previously-cited "second, independent gap" (no per-regime breakdown in `computeStrategyVersionImpact`'s return shape) is closed without changing that function's shape, by directly filtering the same real post-change trades (`ctx.fullJournal` where `ts >= activeVersion.createdAt`, the identical filter `computeStrategyVersionImpact` applies internally) against each trade's own real, already-logged `factorSnapshot.regime.label` (the same field `computeRegimeWinRate`/FM105 already read - not a fabricated new signal). Fires when the active version's own real post-change sample is adequately sized (`afterCount >= 10`, same floor as FM069) but has literally never included a trade under the CURRENT `ctx.regimeLabel`. |
| FM107 | Real risk: a factor's detection logic could reference data not yet available at real decision time | `code-review-level check that no real factor reads a value timestamped after ctx.time` | critical | reduce_confidence | signal_quality | N/A | This is a real, structural/architectural safeguard rather than a live, per-trade check - this app's own real factor functions are built to only reference already-fetched, real, current-refresh data, verified by this project's own extensive test suite rather than checked per-trade. |
| FM108 | Real risk: journal-based historical checks could implicitly exclude real trades that were never completed/logged | `audit whether closed-but-unlogged real trades could skew computeRegimeWinRate/computeFactorPerformance` | low | reduce_confidence | signal_quality | N/A | A form of real survivorship bias if real, incomplete or abandoned trades are systematically excluded from the real journal - this app's own real journal write path (fno_journal_add) is called for every real trade close, including losses, mitigating but not eliminating this real risk. |
| FM109 | Real parameter instability: the same factor's real threshold has been changed recently without a full real re-validation cycle | `compare a factor's current real threshold/weight against its value at the last real strategy-version checkpoint` | low | reduce_confidence | signal_quality | N/A | A real, recently-changed threshold without a full real re-validation cycle carries genuine, real uncertainty about whether the new value is actually better, not just different. |
| FM110 | Real short-term (EMA9/21) and longer-term (EMA21/50) trend signals genuinely disagree | `already covered under Trend Failures - listed again here as it is also, genuinely, a real conflicting-timeframe condition` | medium | require_confirmation | entry | Yes | A real, genuine disagreement between short and longer real trend measures is the clearest, real conflicting-timeframe signal this app can currently compute from its available close-price data. |
| FM111 | Real regime label based on daily-scale volatility genuinely disagrees with the real, sudden intraday volatility-event check | `regime.volatility === 'Low Vol' but computeSuddenVolatilityEvent(...).isSuddenEvent === true` | medium | require_confirmation | entry | Yes | A real, genuine disagreement between the session-scale volatility classification and a real, sudden intraday spike suggests conditions may be genuinely, actively changing faster than the broader regime label reflects. |
| FM112 | Real Market factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Market' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, live price/VIX/breadth-driven Market-category factors means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM113 | Real Flow factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Flow' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, live OI/PCR/option-chain-driven Flow-category factors means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM114 | Real Tech factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Tech' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, close-price-derived technical factors (EMA/RSI/MACD/Bollinger/Fibonacci) means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM115 | Real Vol factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Vol' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, volatility-driven factors (historical vol, IV percentile, VIX-based) means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM116 | Real Costs factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Costs' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, transaction-cost factors (STT/exchange/GST/stamp duty) means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM117 | Real Risk factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Risk' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, journal-derived same-day risk factors (consecutive losses, daily P&L) means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM118 | Real Decay factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Decay' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, Black-Scholes-derived theta/vega/gamma decay factors means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM119 | Real Regulatory factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Regulatory' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, live NSE ban-list/ASM/GSM membership factors means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM120 | Real Microstructure factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Microstructure' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, companion-daemon tick-aggregation factors means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM121 | Real Greeks Deep factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Greeks Deep' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, full Black-Scholes-Merton Greeks and put-call parity factors means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM122 | Real Fundamental factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Fundamental' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, premium-provider-sourced fundamental factors means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM123 | Real Psychology factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Psychology' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, cross-trade behavioral-pattern factors means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM124 | Real Operator Intel factor category is genuinely, largely unavailable this refresh | `classifyUnavailableDataEvent(results).bySources shows a real, high concentration of unavailable factors specifically within the 'Operator Intel' category` | medium | reduce_confidence | data_quality | Yes | A real, concentrated degradation specifically within real, weighted composite OI/PCR/straddle signal means that whole real dimension of the decision is honestly weaker this refresh, not just one isolated factor. |
| FM125 | Real IV percentile at a MILD extreme (75th-90th percentile) | `computeIVPercentileRank(...).percentile is real and between 75-90` | low | reduce_confidence | entry | Yes | A real, mildly elevated IV percentile is a real, softer version of the severe-extreme check above - worth a smaller, real confidence adjustment, not a block. |
| FM126 | Real IV percentile at a SEVERE extreme (98th+ percentile) | `computeIVPercentileRank(...).percentile is real and >= 98` | high | require_confirmation | entry | Yes | A real, severely elevated IV percentile (top 2% of this session's own observed range) is genuinely more extreme than the standard 90th-percentile check - warrants explicit confirmation, not just a soft downgrade. |
| FM127 | Real consecutive losses at a MILD level (2 real losses today) | `Consecutive Losses Today real count === 2` | low | reduce_confidence | risk_management | Yes | A real, 2-loss same-day streak is a real, softer precursor to the documented 3-loss hard block - worth an early, real confidence adjustment. |
| FM128 | Real consecutive losses at a SEVERE level (5+ real losses today) | `Consecutive Losses Today real count >= 5` | critical | block | risk_management | Yes | A real, severe same-day losing streak (well beyond the standard 3-loss threshold) is real, strong evidence something is genuinely wrong with either the setup or the trader's own state today. |
| FM129 | Real regime win rate at a MILD underperformance level (41-50%) | `regime win rate is real and between 41-50%, just above the hard 40% downgrade threshold` | low | reduce_confidence | entry | Yes | A real, mildly-below-average regime win rate is worth a real, small confidence note even though it doesn't clear this app's own documented 40% downgrade threshold. |
| FM130 | Real regime win rate at a SEVERE underperformance level (below 20%) | `regime win rate is real and < 20%, well below the standard 40% threshold` | critical | block | entry | Yes | A real, severely poor regime win rate (less than half the standard downgrade threshold) is real, strong evidence this exact condition should not be traded at all right now, not just downgraded. |
| FM131 | Real bid-ask spread at a MILD widening level | `spread is real and moderately above typical, below the hard simulateOrderRejection threshold` | low | reduce_confidence | execution | Yes | A real, moderately wide spread that doesn't clear this app's own documented hard-rejection threshold is still worth a real, small position-sizing caution. |
| FM132 | Real theta decay at a MILD level (2-5% of premium/day) | `abs(thetaPerDay)/optPrice is real and between 2-5%` | low | reduce_confidence | position_sizing | Yes | A real, moderate daily decay rate is a real, softer version of the severe theta-decay check above. |
| FM133 | Real days-to-expiry genuinely very short (0-1 real days) | `daysToExpiry <= 1` | high | require_confirmation | risk_management | Yes | Real, same-day or next-day expiry means genuinely accelerated theta and real gamma risk - a real, materially different risk profile than a normal multi-day position. |
| FM134 | Real days-to-expiry genuinely far out (30+ real days) | `daysToExpiry >= 30` | low | reduce_confidence | signal_quality | Yes | A real, unusually long-dated option for this app's typical short-term trading pattern carries genuinely different real theta/vega dynamics than its factor weights were primarily calibrated for. |
| FM135 | Real-money trading globally disabled at the source-code level | `FNO_REAL_MONEY_TRADING_GLOBALLY_ENABLED === false` | critical | block | execution | Catalogued only | This app's own real, master, code-level safeguard structurally blocks any real-money order regardless of any other check - by real, deliberate design. |
| FM136 | Real-money account is not genuinely armed | `fno_is_real_money_armed(...) returns false` | critical | block | execution | Catalogued only | A real-money order was attempted against an account that has not been genuinely, explicitly armed via this app's own real confirmation-phrase flow. |
| FM137 | Real-money account was armed under a real, now-stale plugin version | `armed_plugin_version !== current FNO_JOURNAL_SCHEMA_VERSION` | critical | block | execution | Catalogued only | A real software update occurred since this account was armed - this app's own real safeguard automatically, safely disarms it rather than risk stale, unreviewed authorization. |
| FM138 | Real broker for the requested real-money trade is not genuinely implemented | `fno_get_broker_catalog()[broker].implemented === false` | critical | block | execution | Catalogued only | This app honestly has no real, working integration for this broker yet (e.g. Angel One) - never a fabricated order. |
| FM139 | Real-money account is genuinely missing required real credentials | `account api_key_encrypted or access_token_encrypted is empty` | critical | block | execution | Catalogued only | A real-money order cannot be authenticated with the real broker without complete, real, stored credentials. |
| FM140 | Real headless driver secret is genuinely invalid or missing on a request | `hash_equals(fno_get_headless_driver_secret(), provided) === false` | critical | block | execution | Catalogued only | This app's own real, secure, timing-safe authentication check has genuinely failed - never a silently-accepted unauthenticated headless request. |
| FM141 | Real headless driver's own market-hours check reports the market genuinely closed | `isRealMarketHours().isMarketHours === false` | low | block | entry | Catalogued only | The autonomous driver correctly, honestly skips a real trading cycle entirely outside real NSE session hours - not a failure of the setup, a correct, structural no-op. |
| FM142 | Real headless driver's own data fetch for a required source genuinely failed this cycle | `a real fetchJson() call throws inside the driver's runCycle()` | medium | reduce_confidence | data_quality | Catalogued only | This app's own real, standalone driver catches and logs a genuine per-cycle fetch failure without crashing the whole process - the cycle is honestly skipped, retried next real interval. |
| FM143 | Real support/resistance level from prior real session action genuinely being tested | `Support Previous Day Low / Resistance Previous Day High factors (f47/f48) - honestly UNAVAILABLE with current data source` | medium | reduce_confidence | data_quality | N/A | This app honestly cannot compute a real previous-day-low/high level without real intraday high/low data - correctly reports UNAVAILABLE rather than a fabricated level. |
| FM144 | Real weekly high/low level genuinely being tested | `Weekly High/Low factor (f49) - honestly UNAVAILABLE, no reliable week boundary in the chart payload` | low | reduce_confidence | data_quality | N/A | This app honestly cannot reliably compute a real weekly high/low from its rolling chart window - correctly reports UNAVAILABLE. |
| FM145 | Real chart-pattern (flag/triangle/wedge) genuinely forming | `Chart Pattern Flag/Triangle factor (f59) - honestly UNAVAILABLE, needs real trendline touches from true intraday H/L` | low | reduce_confidence | data_quality | N/A | This app honestly cannot reliably detect real chart patterns without true intraday high/low data - correctly reports UNAVAILABLE rather than a fabricated pattern match. |
| FM146 | Real rejection-wick price action genuinely present | `Rejection Wick factor (f56) - honestly UNAVAILABLE, needs real intraday high/low` | low | reduce_confidence | data_quality | N/A | A real rejection wick genuinely cannot be identified from close-only real price data - correctly reports UNAVAILABLE. |
| FM147 | Real Supertrend indicator direction genuinely available | `Supertrend Direction factor (f44) - honestly UNAVAILABLE, needs real intraday high/low` | low | reduce_confidence | data_quality | N/A | Supertrend genuinely requires real intraday high/low (ATR-based) that this app's chart source does not provide - correctly reports UNAVAILABLE rather than a fabricated direction. |
| FM148 | Real ATR-based stop distance genuinely computable | `ATR Stop Distance factor (f55) - honestly UNAVAILABLE, needs real intraday high/low` | low | reduce_confidence | data_quality | N/A | True ATR genuinely requires real intraday high/low - this app correctly, honestly substitutes a real close-price-range proxy elsewhere, but f55 itself correctly reports UNAVAILABLE rather than mislabel the proxy as true ATR. |
| FM149 | Real lot size requested is genuinely not a valid multiple of the real, documented contract lot size | `qty % real, documented lotSize !== 0` | critical | block | position_sizing | Catalogued only | A real order quantity that isn't a genuine multiple of the real, exchange-documented lot size would be structurally invalid at the real broker. |
| FM150 | Real strike price requested does not genuinely exist in this refresh's real option chain | `no real row in ctx.ocRows matches the requested real strike` | critical | block | entry | Yes | A real, nonexistent strike cannot be genuinely priced or traded - never fabricated from a nearby real strike's data. **CLOSED this pass**: found via direct code re-read that the browser's own `ocRow` selection (`assets/fno-lab-core.js`, the `#strike`-input `reduce()`) is a NEAREST-strike match, not an exact one - it silently substitutes the closest real row when the exact typed strike genuinely doesn't exist on the real exchange's strike-step grid, with no signal anywhere that a substitution happened. Wired at the shared gate: a new `requestedStrike` evalCtx field (the real strike each call site's own `tryOpenAutoTradePosition`/driver open-flow was already about to use) is compared against every real row in `ctx.ocRows` for an exact match. The standalone driver (`autonomous-driver.js`) does not have this specific gap - it already uses an exact-match `find()`, never a nearest fallback - but is wired identically anyway, both for real defense-in-depth and so a future code change can't reintroduce a silent nearest-match substitution there undetected. |
| FM151 | Real option type requested is genuinely neither CE nor PE | `optionType not in ['CE','PE']` | critical | block | entry | Yes | A real, structurally invalid option type cannot be mapped to any real, tradeable instrument. |
| FM152 | Real total capital committed across ALL currently open real positions (Auto Trade + every tracked manual position) exceeds a real, documented % of account balance | `sum(position.entryPrice * position.qty for every real open Auto Trade + manual position) / real account balance > a real, documented threshold (e.g. 50%)` | high | block | position_sizing | N/A | Real, genuine overexposure - even if each individual position passed its own real margin/sizing check in isolation, the REAL, combined total across every simultaneously open position can still leave too little real capital as a buffer against an adverse move across multiple positions at once. |
| FM153 | Real, realized slippage on a completed real fill exceeds a real, documented threshold versus the price shown when the decision to trade was made | `abs(fill.price - decisionTimePrice) / decisionTimePrice > a real, documented threshold (e.g. 2%)` | medium | require_confirmation | execution | N/A | This app already, genuinely MODELS the mechanisms that cause slippage (simulateExecutionLatency's real, volatility-scaled price movement between decision and fill; simulateOrderRejection's real liquidity check) but does not yet have its own, explicit, POST-FILL check specifically comparing the realized fill against the original decision-time price as a named failure condition in its own right - a real, honest, buildable gap distinct from the existing, pre-trade latency/rejection checks. |