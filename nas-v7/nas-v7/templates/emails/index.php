<?php if ( ! defined( 'ABSPATH' ) ) exit;
// Email templates are rendered via EmailService::render_template()
// Each template receives: $vars array (booking, client, agency_name, etc.)
// This file is never loaded directly — it's a reference.
?>
