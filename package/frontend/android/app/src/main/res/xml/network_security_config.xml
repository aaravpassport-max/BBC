<?xml version="1.0" encoding="utf-8"?>
<!--
  Allows HTTP to local dev backends (emulator alias, localhost, and LAN IPs).
  Required for physical-device testing against a backend on your computer.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
</network-security-config>
