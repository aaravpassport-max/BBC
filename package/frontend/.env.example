VITE_API_BASE_URL=http://localhost:3000

# Set true only for local demo builds — never in production APKs or hosted web.
# VITE_SHOW_TEST_CREDENTIALS=true

# Enable native FCM on Android when google-services.json is present
# VITE_ENABLE_NATIVE_PUSH=true
